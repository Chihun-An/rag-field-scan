# AGENTS.md — 에이전트 작업 규칙

## 목적
"AI·데이터 분석 × RAG" 분야를 넓게 훑어 정리하는 공개 리서치 저장소.
출발 질문: 규정·품질 문서에 근거를 달아 답하는 AI를 만들려면 무엇을 알아야 할까?

## 작업 방식
- 이 저장소 폴더 안에서만 파일을 읽고 쓴다.
- 작업 전에 AGENTS.md, README.md, worklog.md를 먼저 읽는다.
- 모든 문서는 Markdown(.md)으로 쓴다.
- 파일명은 영문 소문자와 하이픈만 쓴다. (예: areas/01-rag-basics.md)
- 문서 간 링크는 상대 경로로 쓴다. (예: [범위](scope.md))

## 사실과 출처
- 확인하지 않은 사실, 수치, 출처를 만들어내지 않는다.
- 확실하지 않은 내용은 "확인 필요"로 표시한다.
- 주장에는 출처 URL과 확인 날짜를 붙인다.

## 공개 금지
- 지난 과제 원자료(금융사 자료, 품질 내규)와 그 내용
- 멘토링 평가서, 녹취, 사이트 비밀번호
- 실명, 연락처, 계정 정보, API 키
- IATF 관련 내용은 일반적인 문제 유형(문서 식별자, 상호참조, 개정 이력)으로만 쓴다.

## 작업 기록
- 파일을 만들거나 고칠 때마다 worklog.md에 시각, 한 일, 읽은 파일, 만든·고친 파일을 기록한다.
- 에이전트의 실제 오류만 기록한다. 오류를 일부러 만들지 않는다.

## 하지 않는 것
- git commit, push, 저장소 설정 변경, 배포는 하지 않는다. (사람이 직접 한다)
- 파일을 지우기 전에는 사람에게 먼저 묻는다.

## 적용 범위 (에이전트가 여러 개일 때)
- 이 규칙은 이 저장소에서 작업하는 모든 에이전트(Claude Code, Claude Cowork 등)에 똑같이 적용한다.
- 파일 작업은 에이전트가 한다. commit·push, 저장소 설정 변경, 배포는 사람이 한다.
- 사람이 건건이 승인한 경우에만 에이전트가 그 작업을 대신할 수 있고, 누가·언제·무엇을 했는지 worklog.md에 남긴다.
